

<div class="container">
    <div class="row">
        <div class="form-group">
            @using (Html.BeginForm())
            {
                <div class="form-group">
                    @Html.TextBox("searchString");
                </div>

                <div class="form-group">
                    @Html.DropDownList("orders", (SelectList)ViewBag.Orders, new { onchange="this.form.submit();", @class = "form-control" })
                    </div>
                <div class="form-group">
                    <input type="submit" value="SearchStudents" />
                     </div>
                    }
                </div>
            </div>
</div>
